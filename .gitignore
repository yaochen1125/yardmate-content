*.backup_*
.toxicity_audit_cache/

# 整包真源在 ../yardmate-swiftui/scripts/plants_detail.json；此处不再保留，publish.sh 从真源上传 R2
plants_detail.json

# Pass1/Pass2 改名前的逐文件备份，勿入库
*.bak_before_*

# translate_explore_themes.py 断点缓存
.translate_cache/

# 单位换算等批量改写的逐语言快照，勿入库
_backups/
